## Important Notes for Assignment 1

  - **Makefile**:  
  The Makefile is designed to run the main.cpp function for each question. There seem to be a couple of warnings but the code seems to work just fine. Simply changing the required directories for uploading the txt files should suffice and then make can be called to compile and run. The .py files can be run for each individual question 2,3,4 and 5. Just pressing the run button should suffice. In case required to make the Makefile run the .py files as well, the change can be made accordingly.

  - **NSDE pdf files**:  
  Shows the preliminary working and analysis of the problem with pen & paper. Q1 is done completely on here, and questions 2,3,4,5 rough work has been done as well.

### Question 2:
- **plot_Analytical.py**:  
  Make sure to specify the correct directory path when running this file.

- **Running main.cpp**:  
  Simply g++ main.cpp -o file should work incase the Makefile's make command is not working even after changing the directories.
  ./file provides 15 txt files

  - **analysis_parta**:  
  Simple plot of analytical solution.

  - **partb_last_And_c.py**:  
  I hope all the required libraries are installed. Just a simple run should generate the 2 .png files for part b(comparing 0.015625 for the 3 methods, and one comparing the 3 methods' different h values). And there should be 1 .png file for part c, for the error vs stepsize log log graph with reference line.

### Question 3: